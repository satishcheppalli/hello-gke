apiVersion: v1
kind: Service
metadata:
  name: hello-gke
  namespace: default
  labels:
    app: hello-gke
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: hello-gke
